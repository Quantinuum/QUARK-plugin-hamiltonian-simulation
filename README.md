# QUARK-plugin-hamiltonian-simulation

This is a QUARK plugin that implements hamiltonian simulation benchmarks

See the directory `config_examples` for usage examples.

Run a benchmark config using:

```terminal
quark -c <config_file>
```
