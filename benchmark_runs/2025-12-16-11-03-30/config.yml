plugins: [
    "quark_plugin_hamiltonian_simulation",
    ]

pipeline: 
    - "free_fermion_benchmark": {lx: 4, ly: 4, periodic: false, n_trot: 1, two_spin_species: false}
    - "output_qiskit"
    # other modules
