plugins: [
    "quark_plugin_hamiltonian_simulation",
    ]

pipeline: 
    - "free_fermion_benchmark": {lx: 6, ly: 4, periodic: true, n_trot: 3, two_spin_species: false}
    - "output_qiskit"
    # other modules
