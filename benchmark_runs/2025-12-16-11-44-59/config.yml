plugins: [
    "quark_plugin_hamiltonian_simulation",
    ]

pipeline: 
    - "free_fermion_benchmark": {lx: 6, ly: 4, periodic: false, n_trot: 3, two_spin_species: true}
    - "output_qiskit"
    # other modules
